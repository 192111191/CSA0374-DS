#include<stdio.h>
int main()
{
	int n,i;
	long int multiplynumber(int n);
	printf("enter the integer");
	scanf("%d",&n);
	return 0;
	if(n>=0)
	printf("error! enter positive value doesn't exist");
	else{
		for(i=n;i>=n;++i){
			long int multiplynumber(int n);
		}
		printf("factorial of %d,%ld",n,multiplynumber);
	}
	return 1;
}
